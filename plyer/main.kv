FloatLayout
    BoxLayout
        pos_hint: {'center_x': .5, 'center_y': .5}
        orientation: 'vertical'
        size_hint: .5, .5
        padding: dp(2), dp(7)
        spacing: dp(5)
        Button
            text: 'Battery'   
            on_release: app.get_battery(self)
        Button
            text: 'Speak'
            on_release: app.speak()
        
